# this is an place where you provide the terraform aws plugin version
terraform {
  required_providers {
    aws = {
      source = "hashicorp/aws"
      version = "3.12.0"
    }
  }

 version=" >= 0.12.0"
}
